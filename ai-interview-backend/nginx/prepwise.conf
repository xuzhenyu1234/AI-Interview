# Nginx配置文件 - Prepwise
# 放置到服务器的 /etc/nginx/sites-available/prepwise

# HTTP配置 - 用于初始设置和证书验证
server {
    listen 80;
    listen [::]:80;

    # 替换为你的实际域名
    server_name prepwise.example.com;

    # Certbot验证目录
    location /.well-known/acme-challenge/ {
        root /var/www/certbot;
    }

    # 健康检查端点（不重定向）
    location /health {
        proxy_pass http://localhost:8001/api/v1/health;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        access_log off;
    }

    # 其他请求代理到应用（SSL配置前使用）
    location / {
        # 获取SSL证书后，取消下面的注释来启用HTTPS重定向
        # return 301 https://$server_name$request_uri;

        # 临时代理到应用（获取证书前）
        proxy_pass http://localhost:8001;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_cache_bypass $http_upgrade;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;

        # 超时设置
        proxy_connect_timeout 60s;
        proxy_send_timeout 60s;
        proxy_read_timeout 60s;

        # 缓冲设置
        proxy_buffering off;
        proxy_buffer_size 4k;
        proxy_buffers 8 4k;
        proxy_busy_buffers_size 8k;

        # 文件上传大小
        client_max_body_size 50M;
    }
}

# HTTPS配置 - Certbot会在成功获取证书后自动添加
# 这里是参考配置，实际会由Certbot生成
# server {
#     listen 443 ssl http2;
#     listen [::]:443 ssl http2;
#
#     server_name prepwise.example.com;
#
#     # SSL证书路径（Certbot自动配置）
#     ssl_certificate /etc/letsencrypt/live/prepwise.example.com/fullchain.pem;
#     ssl_certificate_key /etc/letsencrypt/live/prepwise.example.com/privkey.pem;
#     include /etc/letsencrypt/options-ssl-nginx.conf;
#     ssl_dhparam /etc/letsencrypt/ssl-dhparams.pem;
#
#     # 安全头
#     add_header Strict-Transport-Security "max-age=31536000; includeSubDomains" always;
#     add_header X-Frame-Options "SAMEORIGIN" always;
#     add_header X-Content-Type-Options "nosniff" always;
#     add_header X-XSS-Protection "1; mode=block" always;
#     add_header Referrer-Policy "strict-origin-when-cross-origin" always;
#
#     # 日志
#     access_log /var/log/nginx/prepwise_access.log combined;
#     error_log /var/log/nginx/prepwise_error.log warn;
#
#     # 根目录处理
#     location / {
#         proxy_pass http://localhost:8001;
#         proxy_http_version 1.1;
#
#         # 代理头设置
#         proxy_set_header Upgrade $http_upgrade;
#         proxy_set_header Connection 'upgrade';
#         proxy_set_header Host $host;
#         proxy_cache_bypass $http_upgrade;
#         proxy_set_header X-Real-IP $remote_addr;
#         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#         proxy_set_header X-Forwarded-Proto $scheme;
#         proxy_set_header X-Forwarded-Host $server_name;
#
#         # WebSocket支持
#         proxy_read_timeout 86400;
#
#         # 超时设置
#         proxy_connect_timeout 60s;
#         proxy_send_timeout 60s;
#
#         # 缓冲设置
#         proxy_buffering off;
#         proxy_buffer_size 4k;
#         proxy_buffers 8 4k;
#         proxy_busy_buffers_size 8k;
#     }
#
#     # API特定配置（可选的限流）
#     location /api/ {
#         # 限流配置（需要在http块中定义zone）
#         # limit_req zone=api_limit burst=20 nodelay;
#
#         proxy_pass http://localhost:8001;
#         proxy_http_version 1.1;
#
#         proxy_set_header Host $host;
#         proxy_set_header X-Real-IP $remote_addr;
#         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#         proxy_set_header X-Forwarded-Proto $scheme;
#
#         # API超时可以设置更长
#         proxy_read_timeout 300s;
#         proxy_connect_timeout 75s;
#     }
#
#     # 静态文件服务（如果有）
#     location /static/ {
#         alias /home/admin/prepwise/static/;
#         expires 30d;
#         add_header Cache-Control "public, immutable";
#         access_log off;
#     }
#
#     # 媒体文件服务（如果有）
#     location /media/ {
#         alias /home/admin/prepwise/media/;
#         expires 7d;
#         add_header Cache-Control "public";
#         access_log off;
#     }
#
#     # 健康检查端点（用于监控）
#     location /health {
#         proxy_pass http://localhost:8001/api/v1/health;
#         proxy_http_version 1.1;
#         proxy_set_header Host $host;
#         access_log off;
#     }
#
#     # 禁止访问隐藏文件
#     location ~ /\. {
#         deny all;
#         access_log off;
#         log_not_found off;
#     }
#
#     # 文件上传大小限制
#     client_max_body_size 50M;
#     client_body_buffer_size 1M;
#
#     # Gzip压缩
#     gzip on;
#     gzip_vary on;
#     gzip_min_length 1024;
#     gzip_types text/plain text/css text/xml text/javascript
#                application/json application/javascript application/xml+rss
#                application/rss+xml application/atom+xml image/svg+xml
#                text/x-js text/x-cross-domain-policy application/x-font-ttf
#                application/x-font-opentype application/vnd.ms-fontobject
#                image/x-icon;
#     gzip_disable "MSIE [1-6]\.";
# }